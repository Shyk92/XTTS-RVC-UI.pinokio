{
  "run": [
    {
      "method": "shell.enter",
      "params": {
        "message": "{{os.platform() === 'darwin' ? 'python3 ./XTTS-RVC-UI/app.py' : 'python ./XTTS-RVC-UI/app.py'}}",
        "on": [
          {
            "event": "Gradio app running on",
            "return": true
          }
        ]
      }
  }
],
  "finalize": [
    {
      "action": "openBrowser",
      "parameters": {
        "url": "http://localhost:7890" 
      }
    }
  ]
}
